<%= form_tag photos_path, multipart: true do |f| %>
  <%= file_field_tag('file', accept: 'image/png, image/gif, image/jpeg') %>

  <%= hidden_field_tag(:album_id, params[:album_id]) %>
  <%= submit_tag %>
<% end %>
